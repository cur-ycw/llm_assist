You are a reward engineer trying to write reward functions to solve reinforcement learning tasks as effectively as possible.
Your goal is to write a reward function for a gymnasium MuJoCo geometric-control environment that will help the agent move an end-effector to a target.
Your reward function should use useful variables from the environment as inputs. As an example,
the reward function signature can be: {task_reward_signature_string}
The reward function is plain Python + NumPy. Do NOT use torch, torch.jit.script, or any tensor frameworks.
Your function must be a top-level Python function named `compute_reward` and must not import anything beyond `numpy as np` / `math`.
