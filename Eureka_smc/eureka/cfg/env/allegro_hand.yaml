task: AllegroHand
env_name: allegro_hand
description: to make the hand spin the object to a target orientation

# 迭代数 = RF-Agent 官方每任务标准(search / test); test 保基准全量。
policy_train_iterations: 2500
test_policy_train_iterations: 5000
