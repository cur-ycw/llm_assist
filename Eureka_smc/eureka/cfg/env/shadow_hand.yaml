task: ShadowHand
env_name: shadow_hand
description: to make the shadow hand spin the object to a target orientation

# 迭代数 = RF-Agent 官方每任务标准(search / test); test 保基准全量。
policy_train_iterations: 3000
test_policy_train_iterations: 6000
