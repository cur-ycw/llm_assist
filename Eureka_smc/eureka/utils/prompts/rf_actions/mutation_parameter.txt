For this particular proposal, keep the STRUCTURE of the reward function fixed and only adjust its numerical parameters. You must keep the same set of reward components and the same environment inputs they depend on: do not add, remove, rename, or re-combine components, and do not change which transformations are applied to them. Restrict your changes to numerical values only -- component weights, thresholds, clipping ranges, scales, and the temperature parameters inside transformations -- so that the reward is better shaped for RL to optimize. Ignore any general suggestion above to rewrite the entire reward function; for this proposal a parameter-only edit is required.
First state, in a single sentence, which parameters you will change and why. Then write the full reward function code.
