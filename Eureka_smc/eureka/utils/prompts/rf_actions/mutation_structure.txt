For this particular proposal, focus on the STRUCTURE of the reward function rather than its numerical constants. You should change the set of reward components and/or how they are combined -- for example, add a new reward component that captures an aspect of the task the current function ignores, remove or replace a component that RL is unable to optimize, or change how the components are gated or coupled together. Do not limit yourself to only tuning existing temperature or scaling constants; the returned dictionary of reward components, the environment signals they use, or the way they are composed should be meaningfully different from the current reward function.
First state, in a single sentence, the structural change you will make and why. Then write the full reward function code.
